services:
  fastapi-app:
    build: .
    container_name: my-fastapi-container
    ports:
      - "8001:8001"
    networks:
      - appnet
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    restart: always

networks:
  appnet:
    external: true
